# Archive Pipeline

This folder preserves the original post-retrieval technical pipeline artifacts for auditability and reproducibility.

Contents include archived versions of:
- `freeze/`
- `master_tables/`
- `monthly_tables/`
- `descriptive_package/`
- `indicator_prep/`
- `workflow/`
- `logs/`
- `project_run_order.txt`

Usage intent:
- Daily research work should use the clean layers in `data/`, `figures/`, and `docs/`.
- Technical tracing and reproducibility checks should reference this archive.

Do not delete this folder unless a full reproducibility-preserving migration is completed.
